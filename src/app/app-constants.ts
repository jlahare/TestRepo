import * as config from '@assets/config/constants.json';

export class AppConstants {
  static CONFIG = config;

  static SUCCESS: string = "success";
  static ERROR: string = "error";
  static WARNING: string = "warning";
}
